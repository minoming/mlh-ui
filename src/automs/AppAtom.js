import {atom} from 'recoil'

export const isAnalyticsLoadingState = atom({
  key: 'isAnalyticsLoadingState',
  default: false
})

export const isLoadingState = atom({
  key: 'isLoadingState',
  default: false
})
